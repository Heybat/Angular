import { LimitSymbolsPipe } from './limit-symbols.pipe';

describe('LimitSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
